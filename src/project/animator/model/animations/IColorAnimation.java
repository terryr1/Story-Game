package project.animator.model.animations;

/**
 * This interface is used for a visitor so that the visitor interface doesn't use any concrete
 * classes. It doesn't need any methods since the animation interface already defines all the
 * functionality of this animation!
 */
public interface IColorAnimation extends AnimationOperations {

}
